!CXX = |> g++ $(CFLAGS) -c %f -o %o |>
!AR = |> ar -cr %o %f |>

ifeq (@(VARIANT),release)
CFLAGS += -O2
endif

ifeq (@(VARIANT),debug)
CFLAGS += -g
endif

CFLAGS += -Wall
CFLAGS += -I$(TUP_CWD)/include/
